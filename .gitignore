# Dependencies
node_modules/

# Next.js generated output
.next/
out/

# TypeScript build cache
*.tsbuildinfo
next-env.d.ts

# Local environment files
.env
.env.*
!.env.example

# Supabase CLI local cache
supabase/.temp/

# Logs and local editor files
*.log
.DS_Store
.vscode/
.idea/

# Claude Code background-agent worktrees — each is its own linked git
# repo, so `git add .` treats it as an embedded repository if it's ever
# left on disk (it's meant to be ephemeral, cleaned up automatically).
.claude/worktrees/

# About page — work in progress, kept local, not shipped yet
/app/about/page.tsx
/app/about/about.css
/components/about/
/content/site/about.ts
